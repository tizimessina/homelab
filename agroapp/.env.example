# Secreto para firmar los JWT. Generar con: openssl rand -hex 32
JWT_SECRET=

# Origen del frontend (CORS). Varios separados por coma.
AGROAPP_WEB_ORIGIN=https://agroapp.home.arpa

# URL de la API que usa el navegador (se compila dentro del frontend).
AGROAPP_API_URL=https://api.agroapp.home.arpa
